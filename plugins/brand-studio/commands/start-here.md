---
description: Start BUILD YOUR BRAND — answer three easy questions and get a full set of brand base-documents in a folder you choose
argument-hint: [optional — a few words about you, e.g. "מאפייה ביתית בשכונה"]
---

Start BUILD YOUR BRAND for this learner:

$ARGUMENTS

Run the start-here skill. Ask which folder to work in, ask the three easy questions (who you are / what you do / your role), then generate the full set of brand base-documents into that folder — brand sheet, brand voice, social seed, welcome points, visual brief, outreach context, carousel outline, and a raw counter-example — and then **stop**. Do not build the email, design system, carousel, or landing page here; each of those is a separate skill the learner runs in its own new window, reading these files from the folder. Everything is about the learner's own world; if they have no business, invent a fitting one and fill it in automatically — never make them work for it, and never fall back to a foreign demo brand. Hebrew-first and RTL-correct, no emojis. When the base set is done, list what was created and tell the learner to open a new window and run the next skill (write-marketing-email / `/marketing-email`).
